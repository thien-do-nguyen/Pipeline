POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=ecommerce
POSTGRES_USER=admin
POSTGRES_PASSWORD=admin
CDC_POSTGRES_PASSWORD=cdc-local-only
KAFKA_PORT=29092
KAFKA_CONNECT_PORT=8083

# Optional local Airflow orchestration. Empty schedule means manual trigger only.
AIRFLOW_PORT=8080
AIRFLOW_BATCH_SCHEDULE=

# Optional: let local Airflow trigger an already-deployed Databricks batch job.
# Leave these empty when you only run local batch orchestration.
AIRFLOW_CLOUD_BATCH_SCHEDULE=
DATABRICKS_HOST=
DATABRICKS_TOKEN=
DATABRICKS_JOB_ID=
DATABRICKS_POLL_SECONDS=20
DATABRICKS_RUN_TIMEOUT_SECONDS=7200
